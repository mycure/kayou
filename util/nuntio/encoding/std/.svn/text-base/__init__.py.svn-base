



import exception


class           c_std:
    def         __init__(self):
        self.name = "std"

    def         encode_int32(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_int32")

    def         encode_uint32(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_uint32")

    def         encode_int64(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_int64")

    def         encode_uint64(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_uint64")

    def         encode_int128(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_int128")

    def         encode_uint128(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_uint128")

    def         encode_double(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_double")

    def         encode_raw(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_raw")

    def         encode_string(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_string")

    def         encode_message_begin(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_message_begin")

    def         encode_message_end(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_message_end")

    def         encode_struct_begin(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_struct_begin")

    def         encode_struct_end(self, id):
        raise exception.NuntioNotImplemented(self.name, "encode_struct_end")

    def         encode_array_begin(self, id, type, dim):
        raise exception.NuntioNotImplemented(self.name, "encode_array_begin")

    def         encode_array_begin(self, id, type, dim):
        raise exception.NuntioNotImplemented(self.name, "encode_array_end")
